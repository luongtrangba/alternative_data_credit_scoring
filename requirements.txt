# ScoreSight — Python dependencies (Python 3.14)
# Cài: pip install -r requirements.txt
#   (môi trường này: pip install --user --break-system-packages -r requirements.txt)

# Data generation + EDA (T1, EDA)
numpy>=2.4
pandas>=3.0
pyarrow>=24.0
matplotlib>=3.10

# Modeling (T3, T4)
scikit-learn>=1.9
lightgbm>=4.6
shap>=0.52

# Serving (T5 — sắp làm)
fastapi>=0.115
uvicorn>=0.30

# UI demo (T6)
streamlit>=1.35
requests>=2.32
