# ScoreSight environment variables

# Set this to use API mode (Streamlit calls FastAPI /score endpoint)
# Leave empty for local mode (Streamlit imports scoring pipeline directly)
SCORE_API_URL=http://localhost:8000/score
